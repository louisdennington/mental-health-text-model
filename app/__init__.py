# Marks a directory as a Python package.
# Why you need it: It allows Python to import files from that folder (e.g. from app.routes import something).
# It can be empty, or you can use it to set up default imports or config when the package is loaded.

